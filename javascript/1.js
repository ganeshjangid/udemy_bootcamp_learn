let name="You must prefix your execution of the *.Js file with the node executable to add context, as if to say,";

let limitCnt=250;

console.log(`Character is left ${parseInt(limitCnt-name.length)}`);

console.log(name.slice(0,1));
